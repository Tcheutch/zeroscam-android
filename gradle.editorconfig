root = true

[*.{kt,kts}]
indent_size = 4
continuation_indent_size = 4
max_line_length = 120

# Règles globales Kotlin (main + test)
# Tu peux ajouter d'autres règles à désactiver ici si besoin
disabled_rules = 

# Configuration spécifique aux fichiers de test
[**/src/test/**.kt]
# On désactive la règle d'ordre des imports uniquement pour les tests
disabled_rules = import-ordering
